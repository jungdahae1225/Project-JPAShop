spring:
#  datasource:
#    url: jdbc:h2:mem:test #DB를 테스트 할 때는 데이터를 남기지 않고 일회성으로 test만 하기 위해 memory용으로 DB를 사용하기 위함
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create
#    properties:
#      hibernate:
##        show-sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace #SQL 실행 파라미터를 로그로 남긴다.#